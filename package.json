{
  "name": "@icw/monorepo",
  "version": "1.0.0",
  "description": "ICW UI Library Monorepo",
  "private": true,
  "keywords": [
    "ui",
    "components",
    "library",
    "monorepo",
    "tailwind",
    "typescript"
  ],
  "scripts": {
    "start": "pnpm run --filter playground start",
    "build": "pnpm run -r build",
    "test": "pnpm run -r test",
    "type-check": "pnpm run -r type-check",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "clean": "pnpm run -r clean"
  },
  "author": "aylon.muramatsu@gmail.com",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0",
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
